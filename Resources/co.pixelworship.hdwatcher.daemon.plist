<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>co.pixelworship.hdwatcher.daemon</string>

    <!-- Resolved relative to the containing app bundle by SMAppService. -->
    <key>BundleProgram</key>
    <string>Contents/MacOS/hdwatcherd</string>

    <!-- A system daemon: starts at boot, before anyone logs in, and runs as
         root so the audit trail lives somewhere the logged-in user cannot
         rewrite. -->
    <key>RunAtLoad</key>
    <true/>
    <key>KeepAlive</key>
    <true/>
    <key>ThrottleInterval</key>
    <integer>10</integer>

    <key>ProcessType</key>
    <string>Background</string>
    <key>LowPriorityIO</key>
    <true/>
    <key>Nice</key>
    <integer>5</integer>

    <key>StandardErrorPath</key>
    <string>/dev/null</string>
    <key>StandardOutPath</key>
    <string>/dev/null</string>
</dict>
</plist>
